import 'package:flutter/material.dart';

class Unknown404 extends StatelessWidget {
  const Unknown404({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Container(
          child: const Text('Unknown404'),
        ),
      ),
    );
  }
}
