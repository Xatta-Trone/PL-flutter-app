import 'package:flutter/material.dart';

class Softwares extends StatelessWidget {
  const Softwares({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Container(
          child: const Text('Softwares'),
        ),
      ),
    );
  }
}
