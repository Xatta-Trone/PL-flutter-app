import 'package:flutter/material.dart';

class RequestPassword extends StatelessWidget {
  const RequestPassword({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Center(
        child: Container(
          child: const Text('RequestPassword'),
        ),
      ),
    );
  }
}
