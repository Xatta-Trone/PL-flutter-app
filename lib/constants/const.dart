import 'package:flutter/material.dart';

const String apiUrl = "https://server.gre-sentence-equivalence.com/api/v1";

const primaryColor = Colors.teal;
const appBgColor = Color(0xFF272727);
const bodyBgColor = Color(0xFF121212);

//bottom navigation bar color
const selectedColor = primaryColor;
const unSelectedColor = Colors.grey;
const bottomBgColor = Colors.white70;

// bottom nav bar icon size
const double iconSize = 18;

//single tutor
const tutorCircleAvatarColor = primaryColor;

//search page

const searchAppbarBg = primaryColor;
